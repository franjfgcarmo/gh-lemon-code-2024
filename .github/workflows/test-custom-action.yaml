name: Test get coomodity price action

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Test Get Commodity Price Action
        uses: franjfgcarmo/get-commodity-price-action@v1.0.0
        with:
          commodity: 'silver'
      - name: Test Get Commodity Price Action
        uses: franjfgcarmo/get-commodity-price-action@v1.0.0
        with:
          commodity: 'gold'
          currency: 'USD'
      - name: Test Get Commodity Price Action
        uses: franjfgcarmo/get-commodity-price-action@v1.0.0
        with:
          commodity: 'silver'
          currency: 'EUR'